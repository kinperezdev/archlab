node_modules/
dist/
*.tsbuildinfo
.DS_Store
.vite/
.env
.env.local

# The global brain is local intellectual property and holds absolute machine
# paths. Never commit its runtime data; keep only the folder's README.
brain/*
!brain/README.md

# Local AI tool configuration is per-machine (permissions, MCP endpoints).
# Applies to contributors' clones too, not just this machine's global ignore.
.claude/
.codex/
